<!DOCTYPE html>
<html lang="tr">

<head>

  <%- include('../partial/head') %>

</head>

<body>

  <%- include('../partial/navbar') %>

    <div class="container-fluid"
      style="background-color: #f0f0f0; height: 100vh; display: flex; justify-content: center; align-items: center;">
      <div class="card shadow-sm" style="width: 100%; max-width: 400px; padding: 20px;">
        <div class="card-body">
          <h2 class="text-center mb-4">Giriş Yap</h2>
          <form action="/auth/login" method="POST">
            <div class="mb-3">
              <label for="email" class="form-label">E-posta</label>
              <input type="email" class="form-control" id="email" name="email" minlength="2" maxlength="50" required>
            </div>
            <div class="mb-3">
              <label for="password" class="form-label">Şifre</label>
              <input type="password" class="form-control" id="password" name="password" minlength="3" maxlength="100" required>
            </div>
            <% if (typeof message !=='undefined' && message) { %>
              <div class="alert alert-<%= message.type %> alert-dismissible">
                <%= message.content %>
              </div>
              <% } %>
                <button type="submit" class="btn btn-success w-100">Giriş Yap</button>
          </form>
          <div class="mt-3 text-center">
            <p>Hesabınız yok mu? <a class="text-primary" href="/auth/register">Kayıt Ol</a></p>
          </div>
        </div>
      </div>
    </div>

    <%- include('../partial/footer') %>

</body>
</html>