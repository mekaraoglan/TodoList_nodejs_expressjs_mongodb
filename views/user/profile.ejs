<!DOCTYPE html>
<html lang="tr">

<head>

<%- include('../partial/head') %>

</head>
<body>
    
    <%- include('../partial/navbar') %>

    <div class="page-container">
        <div class="content-wrap">

    <div class="container mt-5" style="margin-bottom: 200px">
        <div class="row">

            <%- include('../partial/user-account-menu') %>
            
            <div class="col-md-5 personal-info mt-5">
                <h4>Kişisel Bilgilerim</h4>
                <div class="mb-3">
                    <label for="ad" class="fw-bold">Ad:</label>
                    <p id="ad" class="text-muted"><%= user.userFirstName %></p>
                </div>
                <div class="mb-3">
                    <label for="soyad" class="fw-bold">Soyad:</label>
                    <p id="soyad" class="text-muted"><%= user.userLastName %></p>
                </div>
                <div class="mb-3">
                    <label for="email" class="fw-bold">Email:</label>
                    <p id="email" class="text-muted"><%= user.userEmail %></p>
                </div>
                <% if (typeof message !=='undefined' && message) { %>
                    <div class="alert alert-<%= message.type %> alert-dismissible">
                      <%= message.content %>
                    </div>
                    <% } %>
            </div>
        </div>
    </div>
    
</div>
<footer>
    <%- include('../partial/footer') %>
</footer>
</div>

</body>
</html>

